# AThymeToHeal - Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# The public URL of your application
# Development: http://localhost:3000
# Production: https://athymetoheal.org
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Application environment (development, staging, production)
NODE_ENV=development

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these values from: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
# Format: https://[project-id].supabase.co
NEXT_PUBLIC_SUPABASE_URL=

# Supabase anonymous/public key (safe to use in browser)
# This key is used for client-side requests
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase service role key (NEVER expose to browser!)
# This key bypasses Row Level Security - use only in server-side code
# Only needed for admin operations (video uploads, user management)
SUPABASE_SERVICE_ROLE_KEY=

# =============================================================================
# STRIPE PAYMENT CONFIGURATION
# =============================================================================
# Get these values from: https://dashboard.stripe.com/apikeys

# Stripe publishable key (safe to use in browser)
# Use test key (pk_test_...) for development
# Use live key (pk_live_...) for production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Stripe secret key (NEVER expose to browser!)
# Use test key (sk_test_...) for development
# Use live key (sk_live_...) for production
STRIPE_SECRET_KEY=

# Stripe webhook signing secret
# Get this when you create a webhook endpoint
# Development: Use Stripe CLI - run `stripe listen`
# Production: Get from Stripe Dashboard webhook settings
STRIPE_WEBHOOK_SECRET=

# Stripe Price IDs for subscription tiers
# Create these in Stripe Dashboard > Products > Pricing
# Format: price_xxxxxxxxxxxxx
STRIPE_PRICE_ID_BASIC=
STRIPE_PRICE_ID_PREMIUM=
STRIPE_PRICE_ID_VIP=

# =============================================================================
# AIRTABLE CONFIGURATION (Optional - for newsletter signups)
# =============================================================================
# Get these values from: https://airtable.com/account

# Airtable API key (if using Airtable for newsletter management)
AIRTABLE_API_KEY=

# Airtable Base ID (the ID of your Airtable workspace)
AIRTABLE_BASE_ID=

# =============================================================================
# EMAIL SERVICE CONFIGURATION (Optional - for transactional emails)
# =============================================================================
# Choose one: Resend, SendGrid, Mailgun, or AWS SES

# Resend (Recommended - simple, developer-friendly)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=

# SendGrid (Alternative)
SENDGRID_API_KEY=

# Mailgun (Alternative)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=

# AWS SES (Alternative - if using AWS)
AWS_SES_REGION=
AWS_SES_ACCESS_KEY_ID=
AWS_SES_SECRET_ACCESS_KEY=

# =============================================================================
# ANALYTICS & MONITORING (Optional)
# =============================================================================

# Vercel Analytics (included with Vercel deployment)
# No configuration needed - just enable in Vercel dashboard

# Google Analytics 4 (Optional)
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Plausible Analytics (Privacy-friendly alternative)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Sentry Error Tracking (Optional)
SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# =============================================================================
# CLOUDINARY CONFIGURATION (Optional - for image/video optimization)
# =============================================================================
# Get these values from: https://cloudinary.com/console

# Only needed if using Cloudinary for thumbnail generation or image optimization
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# =============================================================================
# CALENDLY INTEGRATION (Optional - for consultation booking)
# =============================================================================
# Get these values from: https://calendly.com/integrations/api_webhooks

# Calendly API token (if integrating consultation booking)
CALENDLY_API_TOKEN=
CALENDLY_EVENT_TYPE_URL=

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================

# Enable debug logging (true/false)
DEBUG=false

# Verbose logging for specific modules
# DEBUG_SUPABASE=false
# DEBUG_STRIPE=false

# =============================================================================
# SECURITY
# =============================================================================

# Secret key for NextAuth.js session encryption (if using NextAuth)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# =============================================================================
# NOTES
# =============================================================================

# 1. Required variables (cannot be empty):
#    - NEXT_PUBLIC_SITE_URL (always needed)
#    - NEXT_PUBLIC_SUPABASE_URL (when using Supabase)
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY (when using Supabase)
#    - STRIPE_SECRET_KEY (when using payments)
#    - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY (when using payments)

# 2. Optional variables:
#    - Everything else can be added as needed

# 3. Public vs Private variables:
#    - Variables starting with NEXT_PUBLIC_ are exposed to the browser
#    - Other variables are only available server-side
#    - NEVER put secret keys in NEXT_PUBLIC_ variables!

# 4. Different environments:
#    - Create .env.local for local development
#    - Use Vercel Environment Variables for production
#    - Keep .env.local.example in version control as a template

# 5. Testing webhooks locally:
#    - Stripe: Use Stripe CLI with `stripe listen --forward-to localhost:3000/api/webhooks/stripe`
#    - Use ngrok for other webhook testing: `ngrok http 3000`

# =============================================================================
# SETUP CHECKLIST
# =============================================================================

# [ ] Copy this file to .env.local
# [ ] Add .env.local to .gitignore (should already be there)
# [ ] Fill in NEXT_PUBLIC_SITE_URL
# [ ] Create Supabase project and add credentials
# [ ] Create Stripe account and add API keys
# [ ] Restart Next.js dev server after adding/changing variables
# [ ] Test that variables are loading: console.log(process.env.NEXT_PUBLIC_SITE_URL)
